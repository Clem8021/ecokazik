<h1>Gestion des utilisateurs</h1>

<table>
  <thead>
    <tr>
      <th>Email</th>
      <th>Admin ?</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.email %></td>
        <td><%= user.admin? ? "✅" : "❌" %></td>
        <td>
          <%= form_with url: admin_user_path(user), method: :patch, local: true do |f| %>
            <%= f.hidden_field :admin, value: !user.admin? %>
            <%= f.submit user.admin? ? "Retirer admin" : "Promouvoir admin" %>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
